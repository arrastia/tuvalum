import styled from 'styled-components';

import { Column } from 'ui/_styles/components/Column';

const Card = styled(Column)`
  max-width: 20rem;
`;

const CardImage = styled('img')``;

export const Styles = { Card, CardImage };
